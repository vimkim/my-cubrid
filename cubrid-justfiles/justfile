set default-list
set minimum-version := "1.55.0"

# Globally accessible cross-work-context launchers live here. Recipes coupled
# to an initialized CUBRID worktree belong in stow/cubrid.

# Generic CUBRID commands are exposed as top-level recipes.
import 'global/justfile'

# Dev2-specific commands are exposed as `dev2::<recipe>`.
mod dev2
